# MozReminder

## Config

Create ``local_config.py``, you can use ``local_config.py.example`` and edit

```bash
HOST =  'mailserver:port'
username = 'username'
password = 'password'
```

## Use
```bash
python reminder.py
```
